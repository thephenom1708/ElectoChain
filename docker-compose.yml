version: '3'

services:
  web:
    build: .
    command: bash -c "python manage.py migrate && python manage.py runserver 0.0.0.0:5000"
    volumes:
      - ./.:/usr/src/app/
    ports:
      - 5000:5000
